# ... existing properties ...

# Enable AndroidX and Jetifier to support AndroidX dependencies (Compose, Activity, Navigation)
android.useAndroidX=true
android.enableJetifier=true

# ... existing properties ...